<UserControl x:Class="TreasureHuntDesktopApplication.FullClient.Views.CreateHuntView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="13*"/>
            <ColumnDefinition Width="37*"/>
        </Grid.ColumnDefinitions>

        <ComboBox Margin="0,10,0,559" Grid.Column="1" Grid.RowSpan="1" ItemsSource="{Binding Path=TreasureHunts}" DisplayMemberPath="HuntName" SelectedItem="{Binding Path=CurrentTreasureHunt, Mode=TwoWay}" Grid.ColumnSpan="1"/>
     
        <Label Content="New Question: " HorizontalAlignment="Left" Margin="24,15,0,0" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Left" Height="23" Margin="10,48,0,0" TextWrapping="Wrap" Text="{Binding NewQuestion}" VerticalAlignment="Top" Width="120"/>
        <Button Content="Add Question" HorizontalAlignment="Left" Margin="24,92,0,0" VerticalAlignment="Top" Width="84" Command="{Binding SaveQuestionCommand}"/>

        <DataGrid Grid.Column="1" AutoGenerateColumns="false" ItemsSource="{Binding Path=Questions}" SelectedItem="{Binding Path=CurrentQuestion}" Margin="0,46,0,0">
           <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Questions.question}" Header="Question"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

            <TextBox  Text="{Binding Path=CurrentQuestion.Question, Mode=TwoWay}" HorizontalAlignment="Left" Height="23" Margin="10,163,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <Label Content="Selected Question:" HorizontalAlignment="Left" Margin="21,128,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.5,0.5">
            <Label.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleX="3.051"/>
                    <RotateTransform/>
                    <TranslateTransform X="0.693"/>
                </TransformGroup>
            </Label.RenderTransform>
        </Label>
        <Button Content="Update Question" Command="{Binding UpdateQuestionCommand}" HorizontalAlignment="Left" Margin="10,207,0,0" VerticalAlignment="Top" Width="114"/>

    </Grid>
</UserControl>
